# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true
        invisible: true

    name:
        label: tiipiik.catalog::lang.brands.name
        searchable: true

    slug:
        label: tiipiik.catalog::lang.brands.slug

    published:
        label: tiipiik.catalog::lang.brands.published
        type: switch