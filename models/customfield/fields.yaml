# ===================================
#  Form Field Definitions
# ===================================

fields:
    template_code:
        label: Template code
        placeholder: Name for using in templates
    
    display_name:
        label: Display name
        placeholder: Display name for fromtend
        
    default_value:
        type: textarea
        label: Default value
        placeholder: Default value
        commentAbove: Default value will be added to products only on custom field creation. Updated value will only be added to the products created after this update.