# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: ID
        searchable: true
        invisible: true
    
    name:
        Label: Name
    
    slug:
        label: Slug
    
    group:
        label: Group
        type: relation
        select: name
        sortable: false
    
    is_activated:
        label: Is activated
        type: switch